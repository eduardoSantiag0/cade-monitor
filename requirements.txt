# -----------------------------------------------------------------------
# CADE Monitor — dependências Python
#
# Versões fixadas em valor exato para builds reprodutíveis (spec
# 002-repo-hardening-cleanup, FR-006). Ao atualizar uma versão aqui,
# rode a suíte de testes localmente antes de commitar a mudança.
# -----------------------------------------------------------------------
Django==5.2.15
gunicorn==26.2.0
python-dotenv==1.2.2
whitenoise==6.12.0
redis==8.1.0
pydantic==2.13.5
sentry-sdk==2.70.0
# Driver PostgreSQL (banco principal em produção). Justificativa: constituição
# v2.0.0, Princípio VIII — driver oficial do Django, sem alternativa em stdlib.
psycopg[binary]==3.3.6
